<!-- 
For administrators should this table show ALL projects?
If yes, then regular users maybe should see their own projects in addition to the public ones.
Guests should just see public projects.
Or should this page JUST show public projects?
-->

<table class='table table-condensed table-striped sortable' >
<thead>
	<tr>		
		<th></th>
		<th>ID</th>
		<th>Name</th>
		<th>Project Title</th>
		<th>P.I. (username)</th>
		<th>Email Address</th>
		<th>Institute</th>
	</tr>
</thead>
<tbody>
<% cnt = 1 %>
<% //for (pid in proj) { %>
<% for (n in sorted_keys){ %>
	<% var pname = sorted_keys[n]; %>
	<% var public = proj[pname].public; %>
	
	<% if(public == 1) { %>
	    <tr>
	    <td><%= cnt %></td>
	    <td><%= proj[pname].pid %></td>
	    <td><a data-toggle="tooltip" data-placement="right" data-html="true" title='<%= proj[pname].description %>' href='/projects/<%= proj[pname].pid %>'><%= proj[pname].project %></a></td>
	    <td><%= proj[pname].title %></td>
	    <% if(proj[pname].username == 'guest') { %>
	    	<td>public (guest)</td>
	    	<td>vamps@mbl.edu</td>
	    	<td>MBL</td>
	    <% }else{ %>
	    	<td><%= proj[pname].last %>, <%= proj[pname].first %> (<%= proj[pname].username %>)</td>
	    	<td><%= proj[pname].email %></td>
	    	<td><%= proj[pname].institution %></td>
	    <% } %>
	    </tr>
	    <% cnt += 1 %>
	<% } %>
	
    
<% }%>
</tbody>
</table>


