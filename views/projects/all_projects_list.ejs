<!-- 
For administrators should this table show ALL projects?
If yes, then regular users maybe should see their own projects in addition to the public ones.
Guests should just see public projects.
Or should this page JUST show public projects?
-->

<table class='table table-condensed table-striped sortable' >
<thead>
	<tr>		
		<th></th>
		<th>ID</th>
		<th>Name</th>
		<th>Project Title</th>
		<th>P.I. (username)</th>
		<th>Email Address</th>
		<th>Institute</th>
	</tr>
</thead>
<tbody>
<% cnt = 1 %>
<% for (pid in proj) { %>
	<% var public = proj[pid].public; %>
	
	<% if(public == 1) { %>
	    <tr>
	    <td><%= cnt %></td>
	    <td><%= pid %></td>
	    <td>	      
	      <a title='<%= proj[pid].description %>' href='/projects/<%= pid %>'> <%= proj[pid].project %> </a>
	    </td>
	    <td>
	      		<%= proj[pid].title %>
	    </td>
	    <td>
	      		<%= proj[pid].last %>, <%= proj[pid].first %> (<%= proj[pid].username %>)
	    </td>
	    <td>
	      		<%= proj[pid].email %>
	    </td>
	    <td>
	      		<%= proj[pid].institution %>
	    </td>
	    </tr>
	    <% cnt += 1 %>
	<% } %>
	
    
<% }%>
</tbody>
</table>


