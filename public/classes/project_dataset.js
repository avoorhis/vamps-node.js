// project_dataset.js 

var express = require('express');
var router = express.Router();


var DATASET = {
	var id,
	var full_name,
	var pname,
	var dname,
	var seq_count,
	var env_sample_source_id,
	var seq_ids: [],


}
module.exports = DATASET;