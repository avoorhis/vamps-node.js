The purpose for this directory will be to contain all the downloaded and saved user data.
Ordered by username (each username will be a directory) each user will access to download their files (metadata, fasta...) and saved dataset lists. The datasets lists can be used in vamps to plug into visualizations and use as a base to search for metadata.
